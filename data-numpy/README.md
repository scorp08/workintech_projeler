Bu challenge’ın amacı Numpy kütüphanesine alışmak, multidimensional array’leri manipüle etmek ve `for` loop kullanmadan bazı linear algebra işlemlerini uygulamaktır. 

Bu challenge için `make`  komutu yok çünkü her şey Notebook içinde gerçekleşecek. Doğru cevabı aldığını, cell altındaki output’a bakarak görsel olarak doğrulayacaksın.

Terminalde şu komutu çalıştır:

```bash
jupyter notebook numpy_warm-up.ipynb
```

İyi eğlenceler!

---

Bu challenge’ı bitirdiğinde commit ve push yapmayı unutma!

```bash
git add numpy_warm-up.ipynb
git commit -m "Numpy 101 Challenge completed"
git push origin master
```
